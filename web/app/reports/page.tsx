import { ComingSoon } from "@/components/layout/ComingSoon";

export default function Page() {
  return (
    <ComingSoon
      round={3}
      visuals={[
        "Every page, the question it answers and what it shows",
      ]}
    />
  );
}