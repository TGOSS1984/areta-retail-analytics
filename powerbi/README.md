# powerbi

Empty until modelling starts. Once it does, this folder holds `areta-retail-analytics.pbip` — created by saving as PBIP from Power BI Desktop, which splits the report and semantic model into version-control-friendly folders/files automatically (no hand-authoring here).

Imports from `data/warehouse`. Theme comes from `branding/theme/`.
